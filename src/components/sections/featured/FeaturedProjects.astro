---
import Container from "../../shared/Container.astro";
import GradiantText from "../../shared/GradiantText.astro";
import SubTitle2 from "../../shared/SubTitle2.astro";
import Title2 from "../../shared/Title2.astro";
import FeaturedProject from "./components/FeaturedProject.astro";
import { projectsSection } from "./data.ts";

export interface Props {
  className?: string;
}
const { className } = Astro.props;
---

<section id="how-we-work" class=`scroll-mt-24 ${className}`>
  <Container
    className="mx-auto flex flex-col items-center justify-center text-center"
  >
    <Title2>{projectsSection.title}</Title2>
    <SubTitle2>
      {projectsSection.subheading1_1}
      <GradiantText> {projectsSection.subheading1_2}</GradiantText>
    </SubTitle2>
  </Container>
  <Container>
    <div class="mt-8 grid sm:grid-cols-2 lg:grid-cols-5 gap-6 lg:gap-8">
      {
        projectsSection.projects.map((project) => {
          return (
            <FeaturedProject
              number={project.number}
              title={project.title}
              description={project.description}
            />
          );
        })
      }
    </div>
  </Container>
</section>
